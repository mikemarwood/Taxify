node_modules/
server/uploads/*
!server/uploads/.gitkeep
server/data/*.db
server/data/*.db-journal
server/data/*.db-wal
server/data/*.db-shm
client/dist/
.env
*.log
.DS_Store

# Android (Capacitor)
client/android/.gradle/
client/android/.idea/
client/android/build/
client/android/app/build/
client/android/capacitor-cordova-android-plugins/build/
client/android/local.properties
client/android/*.apk
client/android/*.aab
# client/public/downloads/taxify.apk is committed intentionally — deployment is
# git-pull based (see deploy.sh) with no separate artifact hosting, so the
# release APK needs to travel with the repo for the download button/updater to work.
